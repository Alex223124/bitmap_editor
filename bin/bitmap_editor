#!/usr/bin/env ruby
require 'pry'
require 'require_all'

require_all 'lib'

parse_input = ParseInput.new(ARGV.first)
commands = parse_input.run

parse_commands = Commands::Parser.new(commands)
parse_commands.run
